import styled from 'styled-components'
import {FollowButton} from "../PageReview/PageReview.css";

export const ButtonWrapper = styled.div`
  text-align: center;
`

export const FollowButtonProfile = styled(FollowButton)`
  margin: 0;
`